name: f3-local

services:
  postgres:
    image: postgres:18@sha256:4aabea78cf39b90e834caf3af7d602a18565f6fe2508705c8d01aa63245c2e20
    container_name: f3-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-f3local}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-f3local}
      POSTGRES_DB: ${POSTGRES_DB:-f3nation}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./scripts/docker/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-f3local} -d ${POSTGRES_DB:-f3nation}"]
      interval: 5s
      timeout: 5s
      retries: 10

  adminer:
    image: adminer:latest@sha256:983261ecc40a4aaf11e25aeda8ef821f5a43a9f024b3967c81cbcc0e6ce43ba3 # 5.4.2
    container_name: f3-adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./scripts/docker/adminer-plugins.php:/var/www/html/index.php:ro
    depends_on:
      postgres:
        condition: service_healthy

  gcs-emulator:
    image: fsouza/fake-gcs-server:latest@sha256:3730da0e31f7e5186a90ec4899dc2c336104e7599df400411392ef17e684c31f # 1.54.0
    container_name: f3-gcs
    restart: unless-stopped
    ports:
      - "9023:9023"
    volumes:
      - gcs_data:/data
    command: -scheme http -port 9023 -public-host localhost:9023 -backend filesystem -filesystem-root /data

  mailpit:
    image: axllent/mailpit:latest@sha256:956d688cbd13dca36c72d8e94c34d6d1cdf4a005aa6aa4ff6edaaac56c34d8df # v1.30.3
    container_name: f3-mailpit
    restart: unless-stopped
    ports:
      - "1025:1025" # SMTP — matches EMAIL_SERVER in .env.docker.example
      - "8025:8025" # Web UI — open http://localhost:8025 to read sent emails

volumes:
  postgres_data:
  gcs_data:
